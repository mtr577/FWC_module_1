#include<stdio.h>
int main()
{
	unsigned int a = -121;
	printf("%u \n",a);
	printf("1st -121 is stored in 32bit memory with , then we returning unsigned so it treates the stored one as unsigned and prints its unsinged value \n");
	return 1;
}
